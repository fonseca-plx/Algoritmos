#include <iostream>

int main() {
    int x, y, l1, h1, l2, h2;
    std::cin >> x >> y;
    std::cin >> l1 >> h1;
    std::cin >> l2 >> h2;

    int maior = l1;

    if (x < l1 || x < l2) {
        if (y < h1 || y < h2) {
            std::cout << 'N' << std::endl;
        }
        if (y > )
    }
    
    return 0;
}